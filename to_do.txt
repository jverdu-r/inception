
- get fixed the yaml and cofig the database and the server
- replace .env variables using admin
- assure that there no host or --link or links
- assure that the containers doesn't start with a command running an infinite loop 
	also any entrypoint or entrypoint scripts
- assure that there are no hacky patches like tail -f, sleep infinity, while true.
- create a Makefile to run the program and launch the docker compose
- assure that the containers do a restart when they crashed
- place the project into a virtual machine 😁️
- have a domain name jverdu-r.42.fr 
- create the wordpress page
- create the tools files for nginx, mariadb and requirements
- do some bonus? explore this option
